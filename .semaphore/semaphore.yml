version: v1.0
name: Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: (s) Currency - Build
    run:
      when: 'branch = ''develop'' and change_in(''/currency/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Setting Docker builder..."
            - docker buildx create --use --driver=docker-container --name=cache-enabled
            - echo "Building dependencies stage..."
            - docker buildx build --load --progress=plain --target=deps-builder currency/
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-currency:latest-dev --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" currency/'
    dependencies: []
  - name: (s) Auth - Build
    run:
      when: 'branch = ''develop'' and change_in(''/auth/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-auth:latest-dev --build-arg GITHUB_USER="$GITHUB_USER" --build-arg GITHUB_PAT="$GITHUB_PAT" --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" auth/'
    dependencies: []
  - name: (s) Realtime - Build
    run:
      when: 'branch = ''develop'' and change_in(''/realtime/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-realtime:latest-dev --build-arg GITHUB_USER="$GITHUB_USER" --build-arg GITHUB_PAT="$GITHUB_PAT" --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" realtime/'
    dependencies: []
  - name: (s) Wallet - Build
    run:
      when: 'branch = ''develop'' and change_in(''/wallet/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-wallet:latest-dev --build-arg GITHUB_USER="$GITHUB_USER" --build-arg GITHUB_PAT="$GITHUB_PAT" --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" wallet/'
    dependencies: []
  - name: (s) Module - Build
    run:
      when: 'branch = ''develop'' and change_in(''/module/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-module:latest-dev --build-arg GITHUB_USER="$GITHUB_USER" --build-arg GITHUB_PAT="$GITHUB_PAT"  --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" module/'
    dependencies: []
  - name: (s) Product - Build
    run:
      when: 'branch = ''develop'' and change_in(''/product/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-product:latest-dev --build-arg GITHUB_USER="$GITHUB_USER" --build-arg GITHUB_PAT="$GITHUB_PAT" --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" product/'
    dependencies: []
  - name: (s) User - Build
    run:
      when: 'branch = ''develop'' and change_in(''/user/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-user:latest-dev --build-arg GITHUB_USER="$GITHUB_USER" --build-arg GITHUB_PAT="$GITHUB_PAT" --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" user/'
    dependencies: []
  - name: (s) EmailTemplate - Build
    run:
      when: 'branch = ''develop'' and change_in(''/emailtemplate/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-emailtemplate:latest-dev --build-arg GITHUB_USER="$GITHUB_USER" --build-arg GITHUB_PAT="$GITHUB_PAT" --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" emailtemplate/'
    dependencies: []
  - name: (s) Email - Build
    run:
      when: 'branch = ''develop'' and change_in(''/email/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-email:latest-dev --build-arg GITHUB_USER="$GITHUB_USER" --build-arg GITHUB_PAT="$GITHUB_PAT" --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" email/'
    dependencies: []
  - name: (s) File - Build
    run:
      when: 'branch = ''develop'' and change_in(''/file/'', {default_branch: ''develop''})'
    task:
      secrets:
        - name: GitHub Credentials
      jobs:
        - name: Build and push
          commands:
            - echo "Checking out..."
            - checkout
            - echo "Logging in to GHCR..."
            - echo $GITHUB_PAT | docker login ghcr.io --username "$GITHUB_USER" --password-stdin
            - echo "Building image and pushing to GHCR..."
            - 'docker buildx build --push --progress=plain -t ghcr.io/autobar-dev/service-file:latest-dev --build-arg GITHUB_USER="$GITHUB_USER" --build-arg GITHUB_PAT="$GITHUB_PAT" --build-arg COMMIT_SHA="$SEMAPHORE_GIT_SHA" --build-arg VERSION="0.0.0-dev" file/'
    dependencies: []
