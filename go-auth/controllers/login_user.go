package controllers

import "github.com/autobar-dev/services/auth/types"

func LoginUser(ac *types.AppContext, email string, password string, remember_me bool) (*types.Tokens, error) {
	return nil, nil
}
